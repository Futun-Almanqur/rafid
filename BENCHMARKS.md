# Benchmarks — Rafid (رافد)

Generated by `eval/report.py` on 2026-09-10 02:45 UTC. Every number below was produced by the
command shown above it. Reproduce any row by running that command.

**Tokenizer used for this run: `APPROX`.** **COLAB VERIFICATION PENDING.** This run used the gateway's approximate byte-based token counter, because `tiktoken` could not download its BPE table in the build environment. Every token, cache-ratio and cost figure below therefore measures the accounting *path*, not real token counts. The Colab run resolves this — the setup cell refuses to start unless the real tokenizer loads, and prints `TOKENIZER=REAL`.

---

## Module 2 — provider comparison

```bash
python scripts/benchmarks.py
```

| Route | Kind | Residency | Model | Overall | ar | en | safety | halalas/call | p50 | p95 |
|---|---|---|---|---|---|---|---|---|---|---|
| `primary` | commercial | cloud | `course-flagship` | 100% | 100% | 100% | 100% | 0.4851 | 2.7 | 2.7 |
| `cheap` | commercial | cloud | `course-small` | 96% | 96% | 95% | 88% | 0.0226 | 2.9 | 3.0 |
| `comparison` | commercial | cloud | `course-anthropic` | 96% | 96% | 95% | 100% | 0.6653 | 1.9 | 2.2 |
| `onprem` | open_weight | on_premise | `wadi-onprem` | 92% | 92% | 91% | 81% | 0.3100 | 3.0 | 3.1 |

**Routing recommendation.** `primary` for the service workflow and for
anything that can refuse; `cheap` behind the cascade for grounded FAQ traffic;
`onprem` for anything a data classification pins on-premise, accepting the
measured quality cost on Arabic and on safety. Recorded with its evidence in
`DECISIONS.md`.

---

## Module 3 — structured outputs

```bash
python -m pytest tests/ -q
```

The strict `StudentRequest` schema rejects an enum violation, a campus outside
the enum, an invented field and a malformed identifier. `extra="forbid"` is what
makes the fourth possible.

---

## Module 4 — guard rates (BOTH numbers)

```bash
python scripts/guard_eval.py
```

| Measure | Cases | Result | Target |
|---|---|---|---|
| attack block rate | 40 | **100.0%** | ≥ 95% |
| legitimate false-positive rate | 40 | **0.0%** | 0% |
| canary leaks | 5 | **0** | 0 |

Reported as a pair, from one run. A block rate without its false-positive pair
is one of the four capping flags and the easiest of the four to avoid.

---

## Module 6 — before / after, with eval verdicts

```bash
python scripts/replay.py && python scripts/benchmarks.py
```

| Step | halalas | per turn | Saved | p50 ms | Eval verdict |
|---|---|---|---|---|---|
| baseline (everything off) | 65.42 | 0.3271 | +0.0% | 2.3 | **green** |
| + prompt cache (stable prefix) | 61.71 | 0.3085 | +5.7% | 2.3 | **green** |
| + exact response cache | 30.56 | 0.1528 | +53.3% | 2.4 | **green** |
| + semantic tier (0.85) | 26.88 | 0.1344 | +58.9% | 2.3 | **green** |
| + cheap-model routing | 2.18 | 0.0109 | +96.7% | 2.3 | **BLOCKED** |
| + cascade (the reversal) | 2.03 | 0.0101 | +96.9% | 2.2 | **green** |

**Meter coverage: 613/613 = 100.0%**, stages `['answer', 'guard', 'router']`. The guard and the router are in the number.

---

## Module 6 — the semantic cache threshold

```bash
python scripts/eval_cache.py
```

| Threshold | Wrong hits | True hits |
|---|---|---|
| 0.7 | 5 | 2 |
| 0.75 | 5 | 2 |
| 0.8 | 1 | 2 |
| 0.85 | 0 | 2 |
| 0.88 | 0 | 2 |
| 0.9 | 0 | 1 |
| 0.92 | 0 | 1 |
| 0.95 | 0 | 1 |

Chosen: **0.85** — the lowest candidate with zero wrong
hits in a live all-vs-all sweep.

---

## Module 6 — throughput and break-even

```bash
python scripts/benchmarks.py
```

Measured: **351.64 req/s** on `onprem` (30 requests in 0.085s).

> **This is throughput against a latency-compressed simulator.** It measures our
> harness, not a model or a GPU. The break-even below is correct in form and
> **unusable as a capacity claim** until re-run against a real served model.

GPU cost assumed: SAR 12.5/hour.

| Compared against | SAR/call | Break-even calls/s | Break-even calls/day |
|---|---|---|---|
| `primary` (`course-flagship`) | 0.010200 | 0.34 | 29,412 |
| `cheap` (`course-small`) | 0.000440 | 7.89 | 681,818 |

Both comparisons are quoted, not only the flattering one.

